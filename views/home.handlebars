<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Student Course Registration</title>
    <style>
        body {
            font-family: Arial, sans-serif;
            margin: 0;
            padding: 0;
            background-color: #f5f5f5;
        }

        h1 {
            text-align: center;
            margin-top: 20px;
        }

        table {
            width: 80%;
            margin: 20px auto;
            border-collapse: collapse;
            background-color: #fff;
            box-shadow: 0 2px 5px rgba(0, 0, 0, 0.1);
        }

        th, td {
            padding: 12px;
            text-align: left;
            border-bottom: 1px solid #ddd;
        }

        th {
            background-color: #4CAF50;
            color: white;
        }

        tr:hover {
            background-color: #f1f1f1;
        }
    </style>
</head>
<body>

<h1>Student Course Registration</h1>

<!-- Таблица для отображения данных о студентах -->
<h2 style="text-align:center;">Student Data</h2>
<table>
    <thead>
    <tr>
        <th>Student ID</th>
        <th>First Name</th>
        <th>Last Name</th>
        <th>Birth Date</th>
        <th>Phone Number</th>
        <th>Email</th>
        <th>City</th>
    </tr>
    </thead>
    <tbody>
    {{#each students}}
        <tr>
            <td>{{student_id}}</td>
            <td>{{first_name}}</td>
            <td>{{last_name}}</td>
            <td>{{birth_date}}</td>
            <td>{{phone_number}}</td>
            <td>{{email}}</td>
            <td>{{city}}</td>
        </tr>
    {{/each}}
    </tbody>
</table>

<!-- Таблица для отображения регистраций на курсы -->
<h2 style="text-align:center;">Course Registrations</h2>
<table>
    <thead>
    <tr>
        <th>Registration ID</th>
        <th>Student ID</th>
        <th>Course ID</th>
        <th>Registration Date</th>
    </tr>
    </thead>
    <tbody>
    {{#each registrations}}
        <tr>
            <td>{{registration_id}}</td>
            <td>{{student_id}}</td>
            <td>{{course_id}}</td>
            <td>{{registration_date}}</td>
        </tr>
    {{/each}}
    </tbody>
</table>

</body>
</html>
